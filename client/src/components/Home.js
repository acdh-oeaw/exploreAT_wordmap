import React from "react";
import { BrowserRouter as Route, NavLink } from "react-router-dom";

const Home = (props)=>{
    return(<div>
        <h2>Home page</h2>
        <h3>DH Explorer </h3>
      </div>);
}

export default Home;